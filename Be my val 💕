<script>
const noBtn = document.getElementById("no");
let speed = 150; // initial move distance

noBtn.addEventListener("mouseover", moveButton);
noBtn.addEventListener("click", moveButton);

function moveButton() {
  // increase speed each time
  speed += 20;

  const x = Math.random() * speed - speed/2;
  const y = Math.random() * speed - speed/2;

  noBtn.style.transform = `translate(${x}px, ${y}px)`;
}

function yesClicked() {
  document.body.innerHTML = `
    <div style="
      height:100vh;
      display:flex;
      justify-content:center;
      align-items:center;
      background:#ff4d6d;
      color:white;
      font-family:Arial;
      text-align:center;
    ">
      <div>
        <h1>YAYYY BELLA!! 💖🥰</h1>
        <p>Bella ❤️ Kosi</p>
        <p>You just made my day 😘
